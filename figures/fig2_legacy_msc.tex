% Fig. 2a - Procedimiento Legacy exitoso
% Compile: pdflatex fig2_legacy_success.tex

\documentclass[border=5pt]{standalone}

\usepackage{amsmath}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}

\begin{document}

\begin{tikzpicture}[
    >=Stealth,
    font=\small,
    entity/.style={
        draw,
        rectangle,
        minimum width=2cm,
        minimum height=0.55cm,
        fill=black!5,
        font=\small\bfseries,
        thick
    },
    msg/.style={
        ->,
        semithick
    },
    sidenote/.style={
        font=\small\itshape,
        text=black!70,
        align=left
    },
    phase/.style={
        font=\small\bfseries\itshape,
        text=black!55
    }
]

% Entidades
\node[entity] (ue) at (0,0) {UE};
\node[entity] (enb) at (8,0) {eNB};

% Líneas de vida
\draw[dashed, black!30, thin] (0,-0.3) -- (0,-10.7);
\draw[dashed, black!30, thin] (8,-0.3) -- (8,-10.7);

% MSG1
\node[phase] at (-2.4,-1.0) {Paso 1};

\draw[msg]
    (0,-1.5)
    --
    node[sloped, above, font=\small]
    {MSG1: Preamble (RAPID $r$)}
    (8,-2.0);

\node[sidenote, anchor=north east] at (-0.3,-1.3)
    {Selecciona un\\preámbulo al azar};

\node[sidenote, anchor=north west] at (8.3,-2.2)
    {Detecta el preámbulo\\por correlación NPRACH};

% MSG2
\node[phase] at (-2.4,-3.2) {Paso 2};

\draw[msg]
    (8,-3.5)
    --
    node[sloped, above, font=\small]
    {MSG2: RAR (RAPID, TC-RNTI, TA, UL Grant)}
    (0,-4.0);

\node[sidenote, anchor=north west] at (8.3,-4.2)
    {Asigna TC-RNTI,\\construye RAR con TA};

% MSG3
\node[phase] at (-2.4,-5.3) {Paso 3};

\draw[msg]
    (0,-5.5)
    --
    node[sloped, above, font=\small]
    {MSG3: RRC Connection Request (TC-RNTI, UE ID)}
    (8,-6.0);

\node[sidenote, anchor=north east] at (-0.3,-5.95)
    {Transmite en el\\recurso PUSCH concedido};

\node[sidenote, anchor=north west] at (8.3,-6.2)
    {Decodifica MSG3};

% MSG4
\node[phase] at (-2.4,-7.3) {Paso 4};

\draw[msg]
    (8,-7.5)
    --
    node[sloped, above, font=\small]
    {MSG4: RRC Connection Setup (C-RNTI)}
    (0,-8.0);

% MSG5
\node[phase] at (-2.4,-9.1) {Paso 5};

\draw[msg]
    (0,-9.3)
    --
    node[sloped, above, font=\small]
    {MSG5: RRC Setup Complete}
    (8,-9.8);

\node[
    font=\small\bfseries,
    text=green!40!black
] at (4,-10.5)
    {Acceso aleatorio completado --- C-RNTI asignado};

\end{tikzpicture}

\end{document}